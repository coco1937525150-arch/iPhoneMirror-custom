namespace IPhoneMirror.App.Services;

internal static class BluetoothHidProtocol
{
    // Increment whenever ReportMap changes incompatibly for an existing pairing.
    internal const int ReportMapVersion = 2;
}
