<ui:FluentWindow x:Class="IPhoneMirror.App.Windows.BluetoothClientBindingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:animations="clr-namespace:IPhoneMirror.UI.Animations"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="{DynamicResource BluetoothClientBindingTitle}"
        Width="520" Height="448" MinWidth="460" MinHeight="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica" WindowCornerPreference="Round"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Grid Style="{StaticResource SubWindowPageRoot}"
          animations:PageTransition.IsEnabled="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Style="{StaticResource SubWindowHeader}"
              animations:WindowDragBehavior.IsEnabled="True">
            <TextBlock Text="{DynamicResource BluetoothClientBindingTitle}"
                       Style="{StaticResource SubWindowTitle}"/>
            <Button Style="{StaticResource SubWindowCloseButton}" Click="OnCancelClick"
                    ToolTip="{DynamicResource Cancel}">
                <ui:SymbolIcon Style="{StaticResource ButtonSymbolIcon}" Symbol="Dismiss20" FontSize="18"/>
            </Button>
        </Grid>
        <StackPanel Grid.Row="2">
            <TextBlock Text="{Binding TargetText}" FontSize="15" FontWeight="SemiBold"/>
            <TextBlock Text="{DynamicResource BluetoothClientBindingHint}"
                       Foreground="{DynamicResource MutedTextBrush}" FontSize="11"
                       TextWrapping="Wrap" Margin="0,5,0,0"/>
        </StackPanel>
        <ListBox Grid.Row="4" ItemsSource="{Binding Clients}"
                 SelectedItem="{Binding SelectedClient, Mode=TwoWay}"
                 BorderThickness="0" Background="Transparent"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,8"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="ItemRoot" Background="{DynamicResource ControlFillBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                        CornerRadius="10" Padding="0">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemRoot" Property="Background"
                                                Value="{DynamicResource ControlHoverBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemRoot" Property="Background"
                                                Value="{DynamicResource ComboItemSelectedBrush}"/>
                                        <Setter TargetName="ItemRoot" Property="BorderBrush"
                                                Value="{DynamicResource AccentBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="ItemRoot" Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource ControlFillBrush}"
                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                            CornerRadius="8" Padding="12,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding ConnectionTimeText}" Foreground="{DynamicResource AccentHoverBrush}"
                                           FontSize="10" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding BindingText}" Foreground="{DynamicResource SuccessBrush}"
                                           FontSize="10" Margin="0,3,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsBound}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="{Binding IdentifierText}" Foreground="{DynamicResource MutedTextBrush}"
                                           FontFamily="Consolas" FontSize="10" Margin="0,4,0,0"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="{DynamicResource BluetoothClientBindingUnbind}"
                                    Click="OnUnbindClick" VerticalAlignment="Center" Margin="12,0,0,0"
                                    Padding="10,5">
                                <Button.Style>
                                    <Style TargetType="Button"
                                           BasedOn="{StaticResource {x:Type Button}}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsBound}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Style="{StaticResource IconButton}"
                    Click="OnRefreshClick" IsEnabled="{Binding CanRefresh}"
                    ToolTip="{DynamicResource BluetoothClientBindingRefresh}"
                    AutomationProperties.Name="{DynamicResource BluetoothClientBindingRefresh}">
                <ui:SymbolIcon Style="{StaticResource ButtonSymbolIcon}"
                               Symbol="ArrowClockwise20" FontSize="18"/>
            </Button>
            <Button Grid.Column="3" Content="{DynamicResource Cancel}" Click="OnCancelClick" MinWidth="88"/>
            <Button Grid.Column="5" Content="{DynamicResource BluetoothClientBindingConfirm}"
                    Click="OnConfirmClick" Style="{StaticResource PrimaryButton}"
                    IsEnabled="{Binding CanConfirm}" MinWidth="132"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
