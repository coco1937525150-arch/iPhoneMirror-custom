<ui:FluentWindow x:Class="IPhoneMirror.App.Windows.AdvancedSettingsWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:animations="clr-namespace:IPhoneMirror.UI.Animations"
 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
 Title="{DynamicResource AdvancedSettingsTitle}" Width="520" Height="450" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica" WindowCornerPreference="Round"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
 <Grid Style="{StaticResource SubWindowPageRoot}" animations:PageTransition.IsEnabled="True">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="16"/><RowDefinition Height="*"/><RowDefinition Height="16"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <Grid Style="{StaticResource SubWindowHeader}"
        animations:WindowDragBehavior.IsEnabled="True">
   <StackPanel Margin="0,0,48,0"><TextBlock Text="{DynamicResource AdvancedSettingsTitle}" Style="{StaticResource SubWindowTitle}"/><TextBlock Text="{DynamicResource AdvancedSettingsIntro}" Style="{StaticResource SubWindowSubtitle}"/></StackPanel>
   <Button Style="{StaticResource SubWindowCloseButton}" Click="OnCloseClick">
    <ui:SymbolIcon Style="{StaticResource ButtonSymbolIcon}" Symbol="Dismiss20" FontSize="18"/>
   </Button>
  </Grid>
  <Border Grid.Row="2" Style="{StaticResource SettingsSection}">
   <StackPanel>
    <TextBlock Text="{DynamicResource AdvancedUsbResolution}" FontWeight="SemiBold"/>
    <TextBlock Text="{DynamicResource AdvancedUsbResolutionHelp}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,6,0,12"/>
    <Grid Margin="0,5,0,0">
     <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="18"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <StackPanel>
      <TextBlock Text="{DynamicResource HeightLabel}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11"/>
      <TextBox x:Name="HeightBox" Style="{StaticResource RoundedTextBox}" Margin="0,5,0,0" FontSize="16" Height="54"/>
     </StackPanel>
     <TextBlock Grid.Column="1" Text="×" Foreground="{DynamicResource MutedTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10"/>
     <StackPanel Grid.Column="2">
      <TextBlock Text="{DynamicResource WidthLabel}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11"/>
      <TextBox x:Name="WidthBox" Style="{StaticResource RoundedTextBox}" Margin="0,5,0,0" FontSize="16" Height="54"/>
     </StackPanel>
    </Grid>
    <TextBlock x:Name="ErrorText" Foreground="{DynamicResource ErrorBrush}" FontSize="11" Margin="0,10,0,0" TextWrapping="Wrap"/>
   </StackPanel>
  </Border>
  <Grid Grid.Row="4"><Button Content="{DynamicResource DisableAdvancedMode}" Width="150" Height="40" HorizontalAlignment="Left" Click="OnDisableClick"/><Button Content="{DynamicResource Apply}" Width="112" Height="40" HorizontalAlignment="Right" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="OnApplyClick"/></Grid>
 </Grid>
</ui:FluentWindow>
